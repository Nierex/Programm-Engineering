# Тема 9.  Концепции и принципы ООП
Отчет по Теме #9 выполнил:
- Донских Александр Сергеевич
- ИВТ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |



## Лабораторная работа №1
Допустим, что вы решили оригинально и немного странно познакомиться с человеком. Для этого у вас должен быть написан свой класс на Python, который будет проверять угадал ли имя человека или нет. Для этого создайте функцию __init__(), а в ней сделайте проверку на то угадал человек ваше имя или нет. Также можете проверить что будет, если в этой функции указать атрибут, который не указан в вашем классе, например, попробуйте вызвать фамилию.
Оформленный код
class Ivan:
    __slots__ = ['name']
    def __init__(self, name):
        if name == "Иван":
            self.name = f"Да, я {name}"
        else:
            self.name = f"Я не {name}, а Иван"

person1 = Ivan('Алексей')
person2 = Ivan('Иван')
print(person1.name)
print(person2.name)

person2.surname = 'Петров'
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_1.1.png)

- Вывод
Класс Ivan использует __slots__ для ограничения атрибутов. При попытке присвоить surname — возникает ошибка, потому что surname не объявлен в __slots__. Это демонстрирует, как __slots__ работает — только разрешённые атрибуты.
## Лабораторная работа №2
Вам дали важное задание, написать продавцу мороженого программу, которая будет писать добавили ли топпинг в мороженое и цену после возможного изменения. Для этого вам нужно написать класс, в котором будет определяться изменили ли состав мороженого или нет. В этом классе реализуйте метод, выводящий на печать «Мороженое с {ТОППИНГ}» в случае наличия добавки, а иначе отобразится следующая фраза: «Обычное мороженое». При этом программа должна воспринимать как топпинг только атрибуты типа string.
Оформленный код
class Icecream:
    def __init__(self, ingredient=None):
        if isinstance(ingredient, str):
            self.ingredient = ingredient
        else:
            self.ingredient = None
    def composition(self):
        if self.ingredient:
            print(f"Мороженое с {self.ingredient}")
        else:
            print("Обычное мороженое")

icecream = Icecream()
icecream.composition()

icecream = Icecream('шоколадом')
icecream.composition()

icecream = Icecream(5)
icecream.composition()
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_1.2.png)

- Вывод
Класс Icecream принимает ingredient и проверяет его тип (str). Если топпинг — строка — выводит "Мороженое с ...", иначе — "Обычное мороженое". Программа корректно обрабатывает числа и другие типы данных.
## Лабораторная работа №3
Петя — начинающий программист и на занятиях ему сказали реализовать их... что-то. А хороший друг Пети и ко всему прочему прекрасно знает, что инкапсуляция — это инкапсуляция, поэтому решает помочь Пети. Ваш класс будет не просто инкапсуляцией, а классом с сеттером, геттером и деструктором. После написания класса вам необходимо продемонстрировать что все написанные вами функции работают. Также вам необходимо объяснить Пете почему на скриншоте ниже в консоли выводится ошибка.
Оформленный код
class MyClass:
    def __init__(self, value):
        self._value = value
    def set_value(self, value): 
        self._value = value
    def get_value(self):  
        return self._value
    def del_value(self):  
        del self._value
    value = property(get_value, set_value, del_value, "Свойство value")

obj = MyClass(42)
print(obj.get_value())
obj.set_value(45)
print(obj.get_value())
obj.set_value(100)
print(obj.get_value())
obj.del_value()
print(obj.get_value())
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_1.3.png)

- Вывод
Класс MyClass использует property() для создания геттера, сеттера и делетера. Ошибка возникает при вызове get_value() после del_value(), потому что атрибут _value удалён. Это показывает, как работает del и почему нельзя обращаться к удалённому атрибуту.
## Лабораторная работа №4
Вам прекрасно известно, что кошки и собаки являются млекопитающими, но компьютер этого не понимает, поэтому вам нужно написать три класса: Кошки, Собаки, Млекопитающие. И при помощи "наследования" объяснить компьютеру что кошки и собаки — это млекопитающие. Также добавьте какой-нибудь свой атрибут для кошек и собак, чтобы показать, что они чем-то отличаются друг от друга.
Оформленный код
class Mammal:
    className = 'Mammal'

class Dog(Mammal):
    species = 'canine'
    sounds = 'wow'

class Cat(Mammal):
    species = 'feline'
    sounds = 'meow'

dog = Dog()
cat = Cat()

print(f"Dog is {dog.className}, but they say {dog.sounds}")
print(f"Cat is {cat.className}, but they say {cat.sounds}")
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_1.4.png)

- Вывод
Классы Dog и Cat наследуются от Mammal. Атрибуты species и sounds показывают различия между животными. Вывод подтверждает, что кошки и собаки — млекопитающие, но издают разные звуки.
## Лабораторная работа №5
На разных языках здороваться по-разному, но суть остается одинаковой, люди друг с другом здороваются. Давайте будем реализуем программу с полиморфизмом, которая будет описывать всю суть первого предложения задачи. Для этого мы можем выбрать два языка, например, русский и английский и написать для них отдельные классы, в которых вы видите атрибут слова, которые здороваться на этих языках. А также напишем функцию, которая будет выводить информацию о том, что для решения поставленной задачи мы использовали декоратор @staticmethod, поскольку нам не нужны обязательные параметры-ссылки вроде self. 
Оформленный код
class Russian:
    @staticmethod
    def greeting():
        print("Привет!")

class English:
    @staticmethod
    def greeting():
        print("Hello!")

def greet(language):
    language.greeting()

ivan = Russian()
greet(ivan)
john = English()
greet(john)
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_1.5.png)

- Вывод
Классы Russian и English используют @staticmethod — методы не требуют экземпляра. Функция greet(language) вызывает метод greeting() у переданного объекта — это полиморфизм: один интерфейс, разные реализации.

## Самостоятельная работа 
Реализовать программу "Садовник", включающую три класса: Tomato, TomatoBush, Gardener.
Класс Tomato описывает один помидор, имеет стадии созревания и методы для роста и проверки созревания.
Класс TomatoBush содержит список объектов Tomato, управляя их ростом и проверкой созревания.
Класс Gardener ухаживает за кустом, переводя помидоры на следующий этап, и собирает урожай, если все плоды созрели.
Также реализовать статический метод справки и протестировать работу всех классов.
- Оформленный код
  class Tomato:
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        self._index = index
        self._state = 0

    def grow(self):
        if self._state < 3:
            self._state += 1

    def is_ripe(self):
        return self._state == 3


class TomatoBush:
    def __init__(self, num_tomatoes):
        self.tomatoes = [Tomato(i) for i in range(num_tomatoes)]
    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()
    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)
    def give_away_all(self):
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        self.name = name
        self._plant = plant
    def work(self):
        print(f"{self.name} работает...")
        self._plant.grow_all()
    def harvest(self):
        if self._plant.all_are_ripe():
            print(f"{self.name} собирает урожай!")
            self._plant.give_away_all()
        else:
            print("Урожай ещё не созрел! Продолжайте ухаживать.")
    @staticmethod
    def knowledge_base():
        print("""
Справка по садоводству:
- Помидор проходит 4 стадии: отсутствует → цветение → зеленый → красный.
- Когда все помидоры красные — можно собирать урожай.
- Если собирать раньше — урожай будет неполным.
""")


# ТЕСТЫ
#1
Gardener.knowledge_base()
#2
bush = TomatoBush(5)
gardener = Gardener("Анна", bush)
#3
print("\n--- Уход за кустом ---")
gardener.work()  # 1-й день
gardener.work()  # 2-й день
gardener.work()  # 3-й день
#4
gardener.harvest()
#5
gardener.work()  # 4-й день
gardener.harvest()

print(f"Количество помидоров на кусте после сбора: {len(bush.tomatoes)}")
![Тема_5](https://github.com/Nierex/Programm-Engineering/blob/main/pic/Lab9_2.1.png)

- Микровывод
Программа демонстрирует применение основных принципов ООП:

Инкапсуляция — скрытие внутреннего состояния (например, _state у помидора).
Объекты связаны — Gardener взаимодействует с TomatoBush, а тот — с Tomato.
Статический метод — knowledge_base() не зависит от экземпляра, содержит справочную информацию.
Работа с коллекциями объектов — TomatoBush управляет списком помидоров.
Логика управления — Gardener может ухаживать и собирать урожай только при определённых условиях.
Задание выполнено полностью: все классы, методы и тесты работают корректно.
- Вывод
В ходе выполнения задач были закреплены основные принципы ООП: инкапсуляция, наследование и полиморфизм. Реализованы классы с атрибутами, методами, статическими функциями и контролем доступа. Показано взаимодействие объектов, управление состояниями и корректная работа с данными.
